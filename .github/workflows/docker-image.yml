name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ pablofdez06 }}
          password: ${{ dckr_pat_SH25goIzpkF0kvnJXPzlObyq3iE }}

      - name: Build image
        run: docker build . \
          --file Dockerfile \
          --tag ${{ pablofdez06 }}/2526_daw_u2_springboot:${TIMESTAMP}

      - name: Push image
        run: docker push ${{ pablofdez06 }}/2526_daw_u2_springboot:${TIMESTAMP}registry.hub.docker.com/${{ pablofdez06 }}/2526_daw_u2_springboot:$(date +%s)